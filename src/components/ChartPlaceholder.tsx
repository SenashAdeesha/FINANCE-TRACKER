function ChartPlaceholder() {
  return (
    <div className="h-64 bg-gradient-to-r from-white to-gray-50 border border-gray-100 rounded-lg flex items-center justify-center text-gray-400">
      <span>Chart Placeholder — add a chart library (Recharts / Chart.js) to render data</span>
    </div>
  );
}

export default ChartPlaceholder;
